{
  "name": "philosoph",
  "version": "0.1.0",
  "private": true,
  "description": "A Kahoot-style classroom game with programmatically generated questions. Free for non-commercial use.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "packageManager": "yarn@4.17.0",
  "workspaces": [
    "module-api",
    "shared",
    "api",
    "student",
    "educator",
    "reports"
  ],
  "scripts": {
    "modules:sync": "node scripts/sync-modules.mjs",
    "build:shared": "yarn build:module-api && yarn workspace @philosoph/shared build",
    "build:student": "yarn workspace @philosoph/student build",
    "build:educator": "yarn workspace @philosoph/educator build",
    "build": "yarn build:shared && yarn build:student && yarn build:educator",
    "build:solo": "yarn build:shared && yarn build:student",
    "start": "node scripts/orchestrate.mjs",
    "dev": "node scripts/orchestrate.mjs",
    "report": "yarn modules:sync && yarn workspace @philosoph/reports report",
    "typecheck": "yarn modules:sync && yarn workspaces foreach -A run typecheck",
    "build:module-api": "yarn workspace @philosoph/module-api build",
    "test": "yarn workspace @philosoph/api test"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.4.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
